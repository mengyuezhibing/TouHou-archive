node_modules/
dist/
*.log
.DS_Store
.env

# 运行时数据（提取出的资源与数据库，体积大且可重建）
# 用 /Data/ 锚定根目录：macOS 文件系统大小写不敏感，
# 不加锚点会连带忽略 docs/data/（发布站点的数据目录）
/Data/
/server/Data/
!server/src/**/*.ts

# 独立的 th06nc-unpack 工具仓库（有自己的远程，不要作为嵌套仓库纳入）
tools/
